import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.otaviobarreto.financeiro',
  appName: 'Financeiro',
  webDir: 'dist',
  android: {
    backgroundColor: '#07182f',
  },
}

export default config
